<UserControl x:Class="HeurePlus.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:HeurePlus.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:HistoryViewModel}">

    <Grid Style="{StaticResource PageRoot}" Margin="32,26,32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Historique" Style="{StaticResource PageTitle}" />
                <TextBlock Text="{Binding CountText}" Style="{StaticResource PageSubtitle}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource SubtleButton}" Content="Rafraîchir" Command="{Binding RefreshCommand}" />
                <Button Style="{StaticResource GhostButton}" Content="Effacer l'historique" Margin="8,0,0,0"
                        Foreground="{DynamicResource Brush.Danger}" Command="{Binding ClearCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="6">
            <Grid>
                <StackPanel Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                            HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontFamily="{StaticResource Font.Icons}" Text="&#xE81C;" FontSize="24"
                               HorizontalAlignment="Center" Foreground="{DynamicResource Brush.TextTertiary}" />
                    <TextBlock Text="Les actions (saisies, exports, sauvegardes…) apparaîtront ici."
                               Style="{StaticResource Muted}" Margin="0,10,0,0" />
                </StackPanel>

                <ListBox ItemsSource="{Binding Items}" Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Focusable" Value="False" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10,9" ToolTip="{Binding Exact}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource Chip}" VerticalAlignment="Center" MinWidth="96">
                                    <TextBlock Text="{Binding Category}" FontSize="11" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Foreground="{DynamicResource Brush.TextMuted}" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="{Binding Description}" Margin="14,0,14,0"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource Brush.HistoryText}" />
                                <TextBlock Grid.Column="2" Text="{Binding When}" VerticalAlignment="Center"
                                           Foreground="{DynamicResource Brush.TextTertiary}" FontSize="12" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
